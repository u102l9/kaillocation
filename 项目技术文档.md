# Kail Location 项目技术文档（小白快速上手版）

> 本文档面向第一次接触本项目的开发者。读完后你应该能回答：项目是干什么的、代码从哪里开始跑、点击界面上的某个按钮会调用哪些函数、四种运行模式各自的原理是什么、Native 注入链是怎么工作的。
>
> 文档中所有路径都是相对仓库根目录 `/home/kail/code/android/kail_location` 的。

---

## 目录

1. [项目是什么](#1-项目是什么)
2. [技术栈与环境要求](#2-技术栈与环境要求)
3. [仓库结构总览](#3-仓库结构总览)
4. [模块与 Gradle 工程](#4-模块与-gradle-工程)
5. [构建命令速查](#5-构建命令速查)
6. [应用启动流程（入口文件 → 主界面）](#6-应用启动流程入口文件--主界面)
7. [整体架构（MVVM + 前台服务 + 注入层）](#7-整体架构mvvm--前台服务--注入层)
8. [四种运行模式详解](#8-四种运行模式详解)
9. [核心调用链：点了"开始模拟"会发生什么](#9-核心调用链点了开始模拟会发生什么)
10. [界面导航与抽屉菜单](#10-界面导航与抽屉菜单)
11. [各功能页面与按钮 → 函数映射表](#11-各功能页面与按钮--函数映射表)
12. [共享基础设施（路线引擎 / 摇杆 / 通知）](#12-共享基础设施路线引擎--摇杆--通知)
13. [Native 层与 Root 注入链](#13-native-层与-root-注入链)
14. [Xposed 伴生模块](#14-xposed-伴生模块)
15. [数据存储与网络](#15-数据存储与网络)
16. [坐标系与地理计算](#16-坐标系与地理计算)
17. [登录鉴权与用量限制](#17-登录鉴权与用量限制)
18. [新人上手路线图](#18-新人上手路线图)
19. [术语表](#19-术语表)
20. [近期代码更新（基于最近 git 提交）](#20-近期代码更新基于最近-git-提交)

---

## 1. 项目是什么

Kail Location 是一个 Android **位置 / 传感器模拟**应用，定位为"调试者的空间坐标实验舱"，用于在合法授权环境下做开发与测试。核心能力：

| 能力 | 说明 |
| --- | --- |
| 位置模拟 | 把设备当前定位改成指定经纬度 |
| 路线模拟 | 录制 / 回放一条路线，沿路线匀速移动 |
| 导航模拟 | 模拟导航过程，观察方向 / 状态变化 |
| 步频模拟 | 伪造计步器 / 加速度传感器数据（跑步、走路） |
| 虚拟定位 | 提供虚拟坐标供定位逻辑验证 |
| NFC 模拟 | 调试场景下的 NFC 能力 |
| WiFi / 基站模拟 | 伪造扫描到的 WiFi 热点与基站信息 |
| 沙盒 | 在 BlackBox 虚拟环境里克隆并运行第三方 App，只对沙盒内 App 生效 |
| Root 隐藏 | 对目标 App 隐藏 Root / 注入痕迹 |

> ⚠️ 合规提示：项目自带《使用条例》，明确仅限授权调试，禁止用于校园跑、打卡、网约车等。开发时请遵守仓库 `README.md` 的条款。

它是一个 **Compose + XML 混合的 MVVM 应用**，并且包含大量 **Native（C++）注入代码**和一个**独立的 Xposed 伴生模块**，因此比普通 App 复杂得多。

---

## 2. 技术栈与环境要求

| 层级 | 选型 | 出处 |
| --- | --- | --- |
| 语言 | Kotlin 2.2.0 + Java 11 + C++17 | `gradle/libs.versions.toml`、`app/build.gradle.kts` |
| UI | Jetpack Compose（BOM 2024.09）+ 少量 XML（viewBinding） | `app/build.gradle.kts` |
| 架构 | MVVM（ViewModel + StateFlow） | `viewmodels/` |
| 数据库 | Room 2.7.0 + 原生 SQLite | `data/local/`、`repositories/` |
| 网络 | OkHttp 4.12 | `network/` |
| 地图 / 定位 | 百度地图 SDK（BD09LL 坐标） | `utils/GoApplication.kt` |
| Native Hook | Dobby 1.2、ShadowHook 1.0.9、自研 ELF/ART Hook | `app/src/main/cpp/` |
| 崩溃监控 | Firebase Analytics / Crashlytics | `app/build.gradle.kts` |
| 沙盒 | NewBlackbox（BlackBoxCore） | `utils/GoApplication.kt`、`sandbox/` |
| Xposed | `de.robv.android.xposed:api:82`（compileOnly） | `KailLocationXposed/app/build.gradle.kts` |

**关键版本号（主 App）**：`applicationId = com.kail.location`，`compileSdk = 36`，`minSdk = 27`，`targetSdk = 36`，`versionName = 1.5.9`，ABI 仅 `arm64-v8a` 与 `armeabi-v7a`。

**Xposed 模块**：`applicationId = com.kail.locationxposed`，`minSdk = 29`。

> ⚠️ 构建坑（来自 `CLAUDE.md`）：根 `settings.gradle.kts` 仍引用 `:NewBlackbox:Bcore`、`:NewBlackbox:black-reflection`、`:NewBlackbox:compiler`、`:NewBlackbox:app`，但这些目录在当前 checkout 中可能缺失。要让根工程同步成功，需要先恢复 `NewBlackbox/` 子树。

---

## 3. 仓库结构总览

```
kail_location/
├── app/                         # 主 App（com.kail.location）
│   ├── build.gradle.kts         # 主 App 构建脚本（含 Native 打包 / slim dex 逻辑）
│   └── src/main/
│       ├── AndroidManifest.xml  # 组件声明、权限、Xposed 包查询
│       ├── assets/inject.dex    # 构建期生成的"精简 dex"注入载荷
│       ├── cpp/                 # 所有 C++ 源码（见 §13）
│       │   ├── CMakeLists.txt
│       │   ├── native_hook/     # 应用内 Dobby 传感器 Hook
│       │   └── root/            # ptrace 注入器 + FakeLocation 还原代码
│       └── java/com/kail/location/
│           ├── auth/            # 登录 / 用量限制
│           ├── data/local/      # Room 数据库
│           ├── geo/             # 地理数学（距离、航向、预测）
│           ├── inject/          # 注入到目标进程后运行的 Java 钩子框架
│           ├── lib/lhooker/     # ART 方法 Hook 的 Java 封装（LHooker）
│           ├── models/          # 数据模型
│           ├── network/         # OkHttp 客户端（更新 / 鉴权 / WiFi 数据）
│           ├── nmea/            # NMEA 报文解析
│           ├── repositories/    # 历史记录仓库
│           ├── root/            # NativeSensorHook（应用内 JNI 桥）
│           ├── sandbox/         # BlackBox 沙盒封装
│           ├── service/         # 四种模式的前台服务（核心！）
│           ├── utils/           # 工具类 + GoApplication
│           ├── viewmodels/      # 所有 ViewModel
│           └── views/           # 所有 Activity + Compose 界面
├── KailLocationXposed/          # 独立 Xposed 模块工程（com.kail.locationxposed）
├── NewBlackbox/                 # 沙盒核心（可能需要恢复）
├── do/                          # FakeLocation 原始反编译源码与还原记录（参考资料）
│   ├── complete/                # 反编译出的 .c 文件 + 重建的 cpp/
│   └── fakelocation/            # 还原记录与设计文档（中文）
├── docs/                        # 文档（本文件所在）
├── CLAUDE.md                    # 给 AI / 新人的项目说明
├── README.md / README_EN.md    # 项目介绍
├── 传感器参数.md                 # 传感器模拟参数说明
├── settings.gradle.kts          # 工程包含关系
└── gradle/libs.versions.toml    # 版本目录
```

> `do/` 目录是**参考资料**，不参与构建。它保存了从原版 FakeLocation 反编译的 C 源码（`do/complete/*.c`）以及把它们重建成可编译 cpp 的记录（`do/fakelocation/还原记录.md` 等）。`app/src/main/cpp/root/` 下的 `.cpp` 就是基于它们重写、改名为 kail 品牌的版本。

---

## 4. 模块与 Gradle 工程

`settings.gradle.kts`（根工程，名为 `location`）包含：

```kotlin
rootProject.name = "location"
include(":app")
include(":NewBlackbox:Bcore")
include(":NewBlackbox:black-reflection")
include(":NewBlackbox:compiler")
include(":NewBlackbox:app")
```

`KailLocationXposed/` 是**另一个独立的 Gradle 工程**（有自己的 `settings.gradle.kts`），不在根工程里，需要单独 `cd KailLocationXposed` 构建。

主 App `app/build.gradle.kts` 里有两段不常见的自定义构建逻辑，务必了解：

1. **Native 可执行文件改名打包**（`tasks.whenTaskAdded { ... mergeXxxNativeLibs ... }`）：
   CMake 产物里有一个名为 `kail_inject` 的**可执行文件**（ptrace 注入器）。Android 打包只接受 `lib*.so`，所以构建脚本把 `kail_inject` 复制并改名为 `libkail_inject.so` 塞进 APK 的 `lib/<abi>/`。运行时 `RootDeployer` 再把它取出来还原成可执行文件用。

2. **Slim DEX 生成**（`androidComponents { onVariants { ... build<Variant>InjectSlimDex } }`）：
   注入到 `system_server` 时，需要一个尽量小的 dex（只含 `com.kail.location.inject.*` 和 `com.kail.location.lib.lhooker.*`）。脚本用 D8/R8 把这些类单独编译成 `inject.dex`（约 1~2MB），放进 `assets/inject.dex`。
   **为什么要这么干**：如果让 `system_server` 里的 DexClassLoader 去加载完整 33MB 的宿主 APK，Android 14 的 ART 校验经常超过 60s，触发 ptrace 看门狗把 `system_server` 杀掉。精简 dex 几毫秒就能加载完。

---

## 5. 构建命令速查

从仓库根目录执行：

```bash
# 主 App
./gradlew assembleDebug              # 打 Debug APK
./gradlew installDebug               # 安装到已连接设备
./gradlew lintDebug                  # 主 App lint
./gradlew testDebugUnitTest          # JVM 单元测试
./gradlew connectedDebugAndroidTest  # 仪器测试
./gradlew :app:assembleRelease       # Release APK
./gradlew :app:buildDebugInjectSlimDex #生成dex

# 跑单个测试类
./gradlew :app:testDebugUnitTest --tests com.kail.location.SomeTest
```

```bash
# Xposed 伴生模块（注意要进子目录）
cd KailLocationXposed && ./gradlew assembleDebug
cd KailLocationXposed && ./gradlew installDebug
```

> 运行日志默认写在设备 `/storage/emulated/0/Documents/KailLocation/logs/`（见 `utils/KailLog.kt`），崩溃日志写在 `getExternalFilesDir("Logs")`（见 `GoApplication.writeCrashToFile`）。

---

## 6. 应用启动流程（入口文件 → 主界面）

### 6.1 Application 入口

**文件**：`app/src/main/java/com/kail/location/utils/GoApplication.kt`
**Manifest 声明**：`<application android:name="com.kail.location.utils.GoApplication" ...>`

`GoApplication` 是整个进程的起点，按以下顺序初始化：

```
GoApplication.attachBaseContext(base)        # 最先执行
 ├─ isMainProcess(this)                       # 判断是否主进程
 └─ BlackBoxCore.get()                         # 初始化沙盒核心
      .closeCodeInit()
      .onBeforeMainApplicationAttach(...)
      .doAttachBaseContext(this, ClientConfiguration{...})  # 配置沙盒：不隐藏Root/不开守护/不用VPN
      .onAfterMainApplicationAttach(...)
      → sandboxInitialized = true

GoApplication.onCreate()
 ├─ BlackBoxCore.get().doCreate()              # 沙盒 doCreate
 ├─ SandboxManager.init(this)                  # 沙盒管理器
 ├─ SandboxSettingsManager.init(this)
 ├─ if (!isMainProc) return                    # 非主进程到此为止
 ├─ PreferenceManager.setDefaultValues(R.xml.preferences_main)  # 读默认设置
 ├─ FirebaseApp.initializeApp(this)
 ├─ AuthManager.init(this)                     # 登录态
 ├─ UsageManager.init(this)                    # 用量限制
 ├─ registerActivityLifecycleCallbacks(this)
 ├─ Thread.setDefaultUncaughtExceptionHandler{...}  # 崩溃落盘到 Logs/crash_*.txt
 └─ SDKInitializer.initialize(this) + setCoordType(BD09LL)  # 百度地图
```

> 要点：**沙盒核心必须在 `attachBaseContext` 里、宿主 Application 真正 attach 之前初始化**，否则沙盒进程无法接管类加载。这就是为什么这部分代码放在 `attachBaseContext` 而不是 `onCreate`。

### 6.2 启动页（权限网关）

**文件**：`app/src/main/java/com/kail/location/views/welcome/WelcomeActivity.kt`
**Manifest**：唯一带 `LAUNCHER` 的是 `LocationSimulationActivity`，但实际首屏是 `WelcomeActivity`（被 `LocationSimulationActivity` 之前的逻辑或快捷方式拉起；它 `exported=true`）。

`WelcomeActivity` 职责：

```
onCreate()
 ├─ PreferenceManager.setDefaultValues(...)        # 尽早生成默认参数
 ├─ 读取 SharedPreferences 里的"协议/隐私"同意状态
 ├─ setContent { WelcomeScreen(...) }              # Compose 界面
 │    ├─ 用户勾选同意 → onCheckedChange
 │    ├─ 点"开始" → startMainActivity(isChecked)
 │    └─ LaunchedEffect → viewModel.checkUpdate(...) # 自动检查更新
 └─ startMainActivity(isChecked):
       checkDefaultPermissions()                   # 申请定位/存储/电话权限
       if (isPermission) → 跳转 LocationSimulationActivity 并 finish()
```

权限请求码 `SDK_PERMISSION_REQUEST = 127`，必需权限为 `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION`，缺失会弹设置引导框 `showPermissionSettingsDialog()`。

### 6.3 主壳 Activity

**文件**：`app/src/main/java/com/kail/location/views/locationsimulation/LocationSimulationActivity.kt`

这是 App 的"主壳"。它继承 `BaseActivity`，持有 `LocationSimulationViewModel`，用 `setContent { LocationSimulationScreen(...) }` 渲染主界面，并把所有抽屉导航项的点击映射成 `startActivity(...)`（见 §10）。

`onResume()` 里有一段**运行模式自动纠偏**逻辑：

```kotlin
if (runMode 不是 root/xposed/sandbox 且 系统已允许模拟位置)
    viewModel.setRunMode("developer")
```

即：如果用户在系统"开发者选项 → 选择模拟位置应用"里选了本 App，会自动把模式设为 developer。

---

## 7. 整体架构（MVVM + 前台服务 + 注入层）

本项目可以拆成**三层**：

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层（Compose Activity + Screen）                           │
│  views/**  —  只负责显示和把点击事件回调给 ViewModel          │
└───────────────────────────┬─────────────────────────────────┘
                            │ StateFlow / 回调
┌───────────────────────────▼─────────────────────────────────┐
│  ViewModel 层                                                 │
│  viewmodels/**  —  持有状态、读写 SharedPreferences、         │
│                    通过 Intent 启动/控制前台服务              │
└───────────────────────────┬─────────────────────────────────┘
                            │ startForegroundService(Intent) / startService(控制指令)
┌───────────────────────────▼─────────────────────────────────┐
│  Service 层（4 种模式的前台服务）                             │
│  service/Developer  service/Root  service/Sandbox  service/Xposed │
│  —  真正干活的地方：跑位置循环、控制摇杆、发通知、下发坐标    │
└───────────────────────────┬─────────────────────────────────┘
                            │ 各模式机制不同（见 §8）
┌───────────────────────────▼─────────────────────────────────┐
│  注入 / Hook 层                                               │
│  developer: LocationManager.addTestProvider（系统标准接口）   │
│  root:      ptrace 注入 system_server + FakeLocation 钩子     │
│  sandbox:   BlackBox 的 BLocationManager                      │
│  xposed:    Xposed 模块 hook + sendExtraCommand("kail") 通信  │
└──────────────────────────────────────────────────────────────┘
```

**关键观念**：UI 几乎不直接操作定位，它只是改 ViewModel 的状态；ViewModel 用 `Intent` 启动对应模式的前台 `Service`；Service 才是各模式差异的真正实现。四种 Service 共享同一套"路线引擎 / 摇杆 / 通知"基础设施（见 §12）。

---

## 8. 四种运行模式详解

运行模式存在 `SharedPreferences` 的 `setting_run_mode` 键里，取值 `root` / `developer` / `xposed` / `sandbox`。在抽屉菜单"运行模式"对话框里切换（见 `views/common/AppDrawer.kt`）。不同模式下：抽屉里可见的菜单项不同、`toggleSimulation()` 启动的 Service 类不同、底层注入机制不同。

### 8.1 Developer 模式（默认，无需 Root）

- **服务**：`service/Developer/ServiceGoDeveloper.kt`
- **机制**：用 Android 官方测试 Provider 接口 `LocationManager.addTestProvider` + `setTestProviderLocation`（封装在 `service/Developer/MockLocationProvider.kt`）。
- **前提**：需要用户在"开发者选项 → 选择模拟位置应用"里选中本 App（`GoUtils.isAllowMockLocation`）。
- **特点**：最安全、最简单，但只能改 App 自己注册的 GPS/NETWORK provider 位置，部分有反作弊的 App 能识别出 `isMock`。

### 8.2 Root 模式（需要 Root）

- **服务**：`service/Root/ServiceGoRoot.kt` + `service/Root/RootDeployer.kt`
- **机制**：综合手段，见 §13。简要说：
  1. `RootDeployer.ensureBaseline()` 把 FakeLocation 全套 `.so`、注入器 `kail_inject`、精简 dex 部署到 `/data/kail-loc/` 和 `/data/local/kail-lib/`；
  2. 用 `appops set <pkg> android:mock_location allow` 帮你自动授予模拟权限（省去开发者选项操作）；
  3. 位置主要仍走 `MockLocationProvider`（和 developer 一样的测试 provider），但 root 帮你搞定了授权；
  4. 可选地 ptrace 注入 `system_server` 注册 `service_mock_*` binder，把 WiFi / 基站 / 反检测 / Root 隐藏等能力打通；
  5. 步频模拟走应用内 `NativeSensorHook`（Dobby hook libsensor）。
- **额外能力**：独立模拟（白名单）、Root/应用隐藏、WiFi 模拟、基站模拟。

> 代码里有一处需要注意的说明 vs 实现差异：`RootDeployer` 类注释强调"**不会**自动 ptrace 注入 system_server"（因为生产 ROM 上容易冻屏），但 `ensureBaseline()` 实际上会 `runCatching { bootstrapInjection() }` 尝试注入（带 5 秒看门狗兜底）。阅读时以代码为准。

### 8.3 Sandbox 模式（无需 Root）

- **服务**：`service/Sandbox/ServiceGoSandbox.kt` + `service/Sandbox/SandboxLocationHook.kt`
- **机制**：基于 NewBlackbox 沙盒。在沙盒里克隆并运行第三方 App，通过 `BLocationManager.get().setGlobalLocation(BLocation(lat,lng))` 给沙盒内**所有** App 注入同一个位置（`GLOBAL_MODE`）。
- **特点**：只影响沙盒内的 App，宿主系统和沙盒外的 App 不受影响；不需要 Root。
- **入口界面**：抽屉的"沙盒"项 → `SandboxActivity`，克隆/启动用 `SandboxManager`。

### 8.4 Xposed 模式（需要 LSPosed / EdXposed 等框架）

- **服务**：`service/Xposed/ServiceGoXposed.kt`
- **伴生模块**：独立 APK `com.kail.locationxposed`（`KailLocationXposed/` 工程，见 §14）。
- **机制**：Xposed 模块在 `system_server`、`com.android.phone` 等进程里 hook 定位 / 电话 / WiFi / GNSS 方法。主 App 与模块之间**通过 `LocationManager.sendExtraCommand("kail", ...)` 这个隐蔽通道通信**（不是广播 / 文件 / socket）。
- **检测模块是否安装**：`AppDrawer.isXposedModuleInstalled()` 用 `PackageManager.getPackageInfo("com.kail.locationxposed")` 判断；没装就弹下载框。

四种模式对比：

| 维度 | Developer | Root | Sandbox | Xposed |
| --- | --- | --- | --- | --- |
| Root | 否 | 是 | 否 | 否（但要 Xposed 框架，框架本身常需 Root） |
| 影响范围 | 全系统（测试 provider） | 全系统 | 仅沙盒内 App | 全系统（hook 系统进程） |
| 反检测能力 | 弱 | 强 | 中 | 强 |
| 额外伴生件 | 无 | Native so + dex | NewBlackbox | 独立 Xposed APK |
| 服务类 | `ServiceGoDeveloper` | `ServiceGoRoot` | `ServiceGoSandbox` | `ServiceGoXposed` |

---

## 9. 核心调用链：点了"开始模拟"会发生什么

这是新人最该搞清楚的一条链路。以**位置模拟主界面**点击"开始"按钮为例。

### 9.1 UI → ViewModel

**界面文件**：`views/locationsimulation/LocationSimulationScreen.kt`
界面上的开始/停止按钮：

```kotlin
Button(
    onClick = onToggleSimulation,                         // ← 点击回调
    colors = ... if (isSimulating) Color.Red else primary,
){ Text(if (isSimulating) "停止" else "开始") }
```

`onToggleSimulation` 在 `LocationSimulationActivity` 里被绑定到 `viewModel::toggleSimulation`。

### 9.2 ViewModel.toggleSimulation()

**文件**：`viewmodels/LocationSimulationViewModel.kt`

```
toggleSimulation()
 ├─ next = !isSimulating
 ├─ 若 next == true（要开始）:
 │   viewModelScope.launch {
 │     UsageManager.canStartSimulation(app)   # 没登录/次数用完 → return（见 §17）
 │     UsageManager.consumeSimulation(app)    # 扣一次免费次数
 │     currentRunMode = prefs["setting_run_mode"]   # 强制读最新模式
 │     MapUtils.bd2wgs(...)                    # 把 BD09 存进历史库（WGS84+BD09都存）
 │     serviceClass = if (root) ServiceGoRoot else ServiceGoDeveloper
 │     intent = Intent(app, serviceClass).apply {
 │         putExtra(LNG_MSG_ID, lng)
 │         putExtra(LAT_MSG_ID, lat)
 │         putExtra(ALT_MSG_ID, 海拔)
 │         putExtra(EXTRA_JOYSTICK_ENABLED, 摇杆开关)
 │         putExtra(EXTRA_COORD_TYPE, "BD09")
 │         if (root) { putExtra(EXTRA_STEP_ENABLED/FREQ, 步频参数) }
 │     }
 │     ContextCompat.startForegroundService(app, intent)   # ★ 启动前台服务
 │     isSimulating = true
 │   }
 └─ 若 next == false（要停止）:
     app.stopService(Intent(app, serviceClass))
     isSimulating = false
```

> 注意：这个主界面的 `toggleSimulation` 只在 `root` 与 `developer` 之间二选一。Xposed / Sandbox 的"开始"分别在各自页面或通过对应 ViewModel 触发（如 `RouteSimulationViewModel.getServiceClass(mode)` 才包含 `xposed`）。

### 9.3 Service 接收 Intent

以 developer 为例，**文件**：`service/Developer/ServiceGoDeveloper.kt`

```
onCreate()
 ├─ mNotificationHelper.initAndStartForeground()  # 必须尽快 startForeground 否则崩
 ├─ mLocManager = getSystemService(LOCATION_SERVICE)
 ├─ initGoLocation()                               # 建一个后台 HandlerThread
 └─ initJoyStick()                                 # 准备摇杆悬浮窗

onStartCommand(intent)
 ├─ 若 intent 带 EXTRA_CONTROL_ACTION（pause/resume/stop/seek/set_speed）→ 走控制分支
 ├─ 否则解析 lat/lng/alt/coordType/速度/路线点
 │   坐标按 coordType 统一转成 WGS84（MapUtils.bd2wgs / gcj02towgs84）
 ├─ mMockLocationProvider.ensureProviders()        # addTestProvider(GPS+NETWORK)
 ├─ startLocationLoop()                             # 启动位置循环
 └─ 按需显示摇杆
```

### 9.4 位置循环（developer）

`initGoLocation()` 里建了个 `Handler`，循环逻辑：

```
handleMessage():
  if (!isStop):
    if (路线激活) mRouteEngine.advance(速度×间隔) → 更新 mCurLat/Lng/Bea
    mMockLocationProvider.setLocation(lat,lng,alt,bea,speed,isStop)  # ★ setTestProviderLocation
    sendEmptyMessageDelayed(自身, 上报间隔ms)   # 默认200ms，由 setting_report_interval 控制
```

`MockLocationProvider.setLocation` 同时给 `GPS_PROVIDER` 和 `NETWORK_PROVIDER` 调用 `setTestProviderLocation`，这就是设备定位被改掉的最终一步。

### 9.5 各模式"最终下发位置"的那一行

| 模式 | 位置循环里调用的关键方法 | 文件 |
| --- | --- | --- |
| Developer | `LocationManager.setTestProviderLocation(...)` | `MockLocationProvider.setLocation` |
| Root | 同上 `MockLocationProvider`（+ 可选注入层 binder `IMockLocationManager`） | `ServiceGoRoot` + `MockLocationProvider` |
| Sandbox | `BLocationManager.get().setGlobalLocation(BLocation(lat,lng))` | `SandboxLocationHook.updateLocation` |
| Xposed | `sendXposedCommand("update_location", {lat,lon})` → 模块 `FakeLocState.updateLocation` | `ServiceGoXposed` + 模块 `KailCommandHandler` |

---

## 10. 界面导航与抽屉菜单

主壳 `LocationSimulationActivity` 用一个 `ModalNavigationDrawer`（抽屉）做导航。抽屉内容由 `views/common/AppDrawer.kt` 渲染。点击抽屉项 → 调用 `onNavigate(R.id.xxx)` → 在 `LocationSimulationActivity.onCreate` 的 `onNavigate` lambda 里 `when(id)` 分发到 `startActivity(...)`。

抽屉项 → 目标 Activity 映射（出自 `LocationSimulationActivity` 的 `onNavigate`）：

| 抽屉项（R.id） | 目标 Activity | 仅在某模式可见 |
| --- | --- | --- |
| `nav_location_simulation` | （当前页，无动作） | 始终 |
| `nav_route_simulation` | `RouteSimulationActivity` | 始终 |
| `nav_navigation_simulation` | `NavigationSimulationActivity` | 始终 |
| `nav_nfc_simulation` | `NfcSimulationActivity` | 始终 |
| `nav_independent_simulation` | `IndependentSimulationActivity` | 仅 root |
| `nav_root_app_hide` | `RootAppHideActivity` | 仅 root |
| `nav_wifi_simulation` | `WifiSimulationActivity` | 仅 root |
| `nav_cell_simulation` | `CellSimulationActivity` | 仅 root |
| `nav_sandbox` | `SandboxActivity` | 仅 sandbox |
| （Xposed 设置） | `XposedSettingsActivity` | 仅 xposed |
| `nav_settings` | `SettingsActivity` | 始终 |
| `nav_sponsor` | `SponsorActivity` | 始终 |
| `nav_contact` | 发邮件 Intent（mailto） | 始终 |
| `nav_source_code` | 浏览器打开 GitHub | 始终 |
| `nav_update` | `viewModel.checkUpdate(this)` | 始终 |

抽屉里的"运行模式"项 → 弹 `showRunModeDialog`，4 个 RadioButton：
- 选 root / sandbox → 直接 `onRunModeChange("root"/"sandbox")`
- 选 developer → `onDeveloperModeSelected()`（先检查是否允许模拟位置，不允许就跳系统开发者设置页）
- 选 xposed → 先 `isXposedModuleInstalled()`，没装则弹下载框

抽屉头部 `DrawerHeader` 有登录入口（点击 → `LoginActivity`）和资料入口（→ `ProfileActivity`）。

**Manifest 中声明的全部 Activity / Service / Provider**（`app/src/main/AndroidManifest.xml`）：

- Activity：`WelcomeActivity`(exported)、`LocationSimulationActivity`(LAUNCHER)、`LocationPickerActivity`、`SettingsActivity`、`HistoryActivity`、`RouteSimulationActivity`、`NavigationSimulationActivity`、`SponsorActivity`、`CheckoutWebViewActivity`、`NfcSimulationActivity`(带 NFC intent-filter)、`IndependentSimulationActivity`、`RootAppHideActivity`、`WifiSimulationActivity`、`WifiPickerActivity`、`CellSimulationActivity`、`XposedSettingsActivity`、`LoginActivity`、`ProfileActivity`、`SandboxActivity`、`SandboxLauncherActivity`(exported, 透明, 用于快捷方式)
- Service：`ServiceGoRoot`、`ServiceGoDeveloper`、`ServiceGoXposed`、`ServiceGoSandbox`（都 `foregroundServiceType="location"`）+ 百度 `com.baidu.location.f`
- Provider：`FileProvider`（authority `${applicationId}.fileProvider`，用于更新包安装）

---

## 11. 各功能页面与按钮 → 函数映射表

每个功能页面都是 `Activity + Screen(Compose) + ViewModel` 三件套。下面按页面列出关键交互。

### 11.1 位置模拟（主界面）

文件：`views/locationsimulation/{LocationSimulationActivity, LocationSimulationScreen}.kt` + `viewmodels/LocationSimulationViewModel.kt`

| 操作 | 调用 | 结果 |
| --- | --- | --- |
| 点"开始/停止"按钮 | `viewModel.toggleSimulation()` | 启停 `ServiceGoRoot`/`ServiceGoDeveloper`（§9） |
| "摇杆"开关 | `viewModel.setJoystickEnabled(b)` | 存 `setting_joystick_enabled`，运行中则发 Intent 更新服务 |
| "步频模拟"开关（仅 root/xposed 可见） | `viewModel.setStepSimulationEnabled(b)` | 存 `setting_step_simulation_enabled` |
| 步频滑块 | `viewModel.setStepCadenceSpm(spm)` | 存 `setting_step_cadence_spm`（60~180 步/分） |
| 点历史记录卡片 | `viewModel.selectRecord(record)` | 把该坐标设为目标位置 |
| 卡片"编辑"图标 | `viewModel.renameRecord(id, name)` | 重命名历史记录 |
| 卡片"删除"图标 | `viewModel.deleteRecord(id)` | 删除历史记录 |
| 右上"+"FAB | `onAddLocation()` → `startActivity(LocationPickerActivity)` | 进地图选点页 |
| 左上菜单图标 | 打开抽屉 | 导航 |

### 11.2 路线模拟

文件：`views/routesimulation/*` + `viewmodels/RouteSimulationViewModel.kt`

| 操作 | 调用 | 结果 |
| --- | --- | --- |
| 搜索地点 | `viewModel.search(keyword, city)` | 百度 `SuggestionSearch` 联想 |
| 选搜索结果 | `viewModel.selectSearchResult(lat,lng)` | 地图标点 |
| 保存路线 | `viewModel.saveRoute(points)` | 存进 `SharedPreferences["saved_routes"]`（JSON），反查地名 |
| 选路线 | `viewModel.selectRoute(id)` | 记住选中 |
| 开始模拟 | `viewModel.startSimulation()` | 据 `runMode` 选 `getServiceClass()`（root/xposed/developer），把路线点数组 + 速度 + 循环 + 步频参数塞进 Intent，`startForegroundService` |
| 停止 | `viewModel.stopSimulation()` | `stopService` |
| 暂停/继续 | `viewModel.pauseSimulation()` / `resumeSimulation()` | 发 `EXTRA_CONTROL_ACTION=pause/resume` |
| 改速度 | `viewModel.updateSpeed(speed)` | 存盘 + 运行中发 `set_speed` 控制指令 |
| 循环开关 | `viewModel.updateLoop(b)` | 存盘 |
| 速度波动开关 | `viewModel.updateSpeedFluctuation(b)` | 存盘 + 运行中发 `set_speed_fluctuation` |
| 步频开关/滑块 | `viewModel.updateStepFreqSimulation(b)` / `updateStepCadenceSpm(spm)` | 仅 root/xposed 生效，发 `set_step` 控制指令 |

> `RouteSimulationViewModel.getServiceClass(mode)` 是**唯一**三模式齐全（root/xposed/developer）的派发点，值得作为理解"模式 → 服务"映射的范本。

### 11.3 独立模拟（仅 root）

文件：`views/independentsimulation/*` + `viewmodels/IndependentSimulationViewModel.kt`

作用：维护一份"目标 App 白名单"。开启后，之后在位置/路线/WiFi/基站/步频页点"开始"，模拟数据**只对白名单里的 App 生效**，其它 App 读真实数据。

| 操作 | 调用 | 结果 |
| --- | --- | --- |
| 开启独立模拟 | `viewModel.setEnabled(true)` | 先过 `UsageManager` 校验，存 `independent_enabled`，若 `ServiceGoRoot.isRunning` 则发 `CONTROL_SET_ALLOW_PACKAGES` 把白名单热更新进注入层 |
| 选/改目标 App | `viewModel.setTargetPackages(csv)` | 存 `independent_target_packages`，运行中实时下发 |

底层：白名单经 `ServiceGoRoot` 下发到 FakeLocation 注入层的 `setAllowMockPackages`（`service_mock_location` / `service_mock_wifi` binder）。白名单为空 = 对所有 App 生效。

### 11.4 其它页面速查

| 页面 | Activity | ViewModel | 关键能力 |
| --- | --- | --- | --- |
| 导航模拟 | `NavigationSimulationActivity` | `NavigationSimulationViewModel` | 模拟导航过程 |
| NFC 模拟 | `NfcSimulationActivity` | `NfcSimulationViewModel`(+Contract) | NFC 标签模拟，带 MVI 风格 Contract |
| WiFi 模拟 | `WifiSimulationActivity`/`WifiPickerActivity` | `WifiSimulationViewModel` | 伪造扫描 WiFi 列表（root） |
| 基站模拟 | `CellSimulationActivity` | `CellSimulationViewModel` | 伪造基站信息（root），数据可从 OpenCellId/Wigle 拉 |
| Root/应用隐藏 | `RootAppHideActivity` | `RootAppHideViewModel` | 对目标 App 隐藏 Root/注入痕迹（root） |
| 沙盒 | `SandboxActivity`/`SandboxLauncherActivity` | `SandboxViewModel` | 克隆/启动沙盒 App |
| 设置 | `SettingsActivity` | `SettingsViewModel` | 全局参数（上报间隔、精度、地图缩放、各种 hook 开关） |
| 历史 | `HistoryActivity` | `HistoryViewModel` | 历史位置/搜索记录 |
| 地图选点 | `LocationPickerActivity` | `LocationPickerViewModel` | 选目标坐标 |
| 赞助 | `SponsorActivity`/`CheckoutWebViewActivity` | `SponsorViewModel` | 赞助 / 订阅 |
| 登录/资料 | `LoginActivity`/`ProfileActivity` | `WelcomeViewModel` 等 | 账号体系 |
| Xposed 设置 | `XposedSettingsActivity` | — | 配置 Xposed 各开关 |
| 摇杆悬浮窗 | （无 Activity，悬浮窗） | `JoystickViewModel` | 手动方向控制 / 路线控制 |

---

## 12. 共享基础设施（路线引擎 / 摇杆 / 通知）

四个前台服务（Developer / Root / Sandbox / Xposed）共用一套基础设施，集中在 `utils/service/` 与 `views/joystick/`。

### 12.1 Intent 常量中枢

**文件**：`utils/service/ServiceConstants.kt`

所有服务的 Intent extra / action / 默认值都定义在这里，各服务 `companion object` 只是转引用，保证一致：

```kotlin
DEFAULT_LAT = 36.667662   DEFAULT_LNG = 117.027707   DEFAULT_ALT = 55.0
EXTRA_ROUTE_POINTS / EXTRA_ROUTE_LOOP / EXTRA_JOYSTICK_ENABLED / EXTRA_ROUTE_SPEED
EXTRA_COORD_TYPE / EXTRA_CONTROL_ACTION / EXTRA_SPEED_FLUCTUATION / EXTRA_SEEK_RATIO
CONTROL_PAUSE="pause" / CONTROL_RESUME="resume" / CONTROL_STOP="stop"
CONTROL_SEEK="seek" / CONTROL_SET_SPEED="set_speed" / CONTROL_SET_SPEED_FLUCTUATION
COORD_WGS84 / COORD_BD09 / COORD_GCJ02
ACTION_STATUS_CHANGED = "com.kail.location.service.STATUS_CHANGED"   # 服务→UI 状态广播
```

控制指令的发送方式：UI/摇杆通过 `Intent(...).putExtra(EXTRA_CONTROL_ACTION, "pause")` 再 `startService()`，服务在 `onStartCommand` 里检测到 `EXTRA_CONTROL_ACTION` 非空就走控制分支（不走"开始新模拟"分支）。

### 12.2 路线引擎

**文件**：`utils/service/RouteEngine.kt`

纯逻辑类，负责沿路线点前进。要点：
- `setupFromArray(routeArray, coordType)`：把 `[lng,lat,lng,lat,...]` 数组按坐标系转成内部 WGS84 点列，算累计距离。
- `advance(distanceMeters)`：沿当前线段推进给定米数，跨段自动跳到下一段，到终点按 `routeLoop` 决定是否回到起点。
- `seekToRatio(ratio)`：拖动进度条时跳到路线某百分比位置。
- 输出 `currentLat/currentLng/currentBea` 给服务，服务再下发。
- `progressRatio` / `buildStatusString()`：给摇杆 UI 显示"已走 / 总长"。

距离计算用 `geo/GeoMath.kt` 的 `metersPerDegLat/Lng` + 勾股近似。

### 12.3 摇杆悬浮窗

**文件**：`views/joystick/JoystickWindowManager.kt`（+ `JoystickRoot/JoystickMainOverlay/JoystickAuxOverlays/FloatingNavigationControlOverlay.kt`，状态在 `viewmodels/JoystickViewModel.kt`）

- 用 `WindowManager` 添加一个 `TYPE_APPLICATION_OVERLAY`（需"显示在其它应用上层"权限）的 `ComposeView`。
- 因为悬浮窗没有 Activity 生命周期，自建了 `FloatingLifecycleOwner`（实现 `LifecycleOwner`/`ViewModelStoreOwner`/`SavedStateRegistryOwner`）让 Compose 能跑。
- 内嵌百度 `MapView`（主图 + 路线图）。
- 手动模式：摇杆方向 → `processDirection(auto, angle, r)` → 定时器周期性回调 `listener.onMoveInfo(speed, disLng, disLat, angle)`，服务据此用 `GeoPredict.nextByDisplacementKm` 推算下一个坐标。
- 路线模式：`showRouteControl(speed)`，显示暂停/继续/进度条，回调通过 `onRouteControl/onRouteSeek/onRouteSpeedChange` 发控制 Intent 给服务。

服务里实现 `JoystickViewModel.ActionListener` 的就是各 `ServiceGoXxx.initJoyStick()`。

### 12.4 前台通知

**文件**：`utils/service/ServiceNotificationHelper.kt`

封装通知渠道创建、`startForeground`、以及通知上的"显示/隐藏摇杆"按钮（action `ACTION_JOYSTICK_SHOW/HIDE`）。每个服务给它不同的 `channelId` 和 `noteId`（Developer=1、Sandbox=1、Root=3）。

> 重要约束：前台服务必须在 `onCreate`/`onStartCommand` 尽快 `startForeground`，否则会触发 `ForegroundServiceDidNotStartInTimeException` 崩溃。这就是各服务 `onCreate` 第一步就 `mNotificationHelper.initAndStartForeground()` 的原因。

---

## 13. Native 层与 Root 注入链

这是项目最复杂的部分，全部在 `app/src/main/cpp/`。原始来源是反编译的 FakeLocation（`do/complete/*.c`），重写成 kail 品牌的可编译 cpp。

### 13.1 CMake 产物

**文件**：`app/src/main/cpp/CMakeLists.txt`

| 产物 | 源文件 | 作用 |
| --- | --- | --- |
| `libkail_native_hook.so` | `native_hook/sensor_simulator.cpp` + `native_hook/hook.cpp`，链接 Dobby | **应用内**步频/传感器 Hook（Dobby 钩 `libsensor.so`/`libsensorservice.so`） |
| `kail_inject`（可执行文件，打包成 `libkail_inject.so`） | arm64=`root/inject64.cpp`，arm=`root/inject.cpp` | **ptrace 注入器** |
| `libfakeloc_init.so` | `root/libfakeloc_init.cpp` | 注入到普通进程/`system_server` 的加载器，反射调 `InjectDex.init` |
| `libfakeloc_initzygote.so` | `root/libfakeloc_initzygote.cpp` | 注入到 zygote 的加载器，反射调 `InjectDex.initZygote` |
| `libfakeloc_apphook.so` | `root/libfakeloc_apphook.cpp` | 注入到目标 App 进程的加载器，反射调 `InjectDex.hookApplication` |
| `liblhooker.so`（OUTPUT_NAME 去前缀） | `root/liblhooker.cpp` | **ART 方法 Hook 引擎**（改 ArtMethod entry point） |
| `libStepSensor.so` | `root/libStepSensor.cpp` | 注入后用的传感器 Hook（ELF PLT/GOT 钩 `libsensorservice.so`） |
| `libantidetect.so` | `root/libantidetect.cpp` | 反检测（ELF 钩 libc 文件系统调用，藏起框架自身文件） |

公共头文件：`root/fakeloc_common.h`（MD5/签名校验/JNI 辅助）、`root/elf_hooker.h`（ELF PLT/GOT Hook，ABI 中立）。

### 13.2 ptrace 注入链（root 模式核心）

注入器命令格式：`kail_inject -P <进程名> -l <加载器.so> -n com.kail.location`

`inject64.cpp` 的 `injectLibraryIntoProcess(pid, lib, arg)` 流程：

```
1. PTRACE_ATTACH 目标进程 + SIGSTOP
2. 解析 /proc/<pid>/maps，识别运行时（stock Android / VMOS / Twoyi / VPhoneGaGa）
3. 用 ASLR 偏移算出远端 calloc/free/dlopen/dlerror 以及 AndroidRuntime::mJavaVM 地址
4. writeRemoteString(lib)  —— 把 .so 路径写进目标进程内存
5. callRemoteFunction(dlopen, 远端路径, RTLD_NOW)   —— 让目标进程加载我们的 .so
6. callRemoteFunction(doRun, &javaVm, "Kail.")        —— 调用 .so 导出的 doRun
7. PTRACE_DETACH
```

**看门狗**：`callRemoteFunction` 有 `gCallTimeoutMs`（默认 5 秒）。如果远端函数卡住（典型是远端 dlopen 被兄弟线程持有的 linker 锁阻塞），到时间就 `PTRACE_DETACH` 让目标进程恢复可运行，避免把 `system_server` 永久冻死（代价是本次"Inject fail"）。

每个加载器 `.so` 都导出 `doRun(JavaVM**, char*)`：
```
doRun → AttachCurrentThread → init(env)
init(env):
  - 校验 payload MD5 / 签名（fakeloc_common.h，当前默认关闭）
  - 在 /data/kail-loc/libfakeloc.so 上建类加载器
  - 反射加载 com.kail.location.inject.fakelocation.InjectDex
  - 调对应入口：init() / initZygote() / hookApplication()
```

`libfakeloc_init.cpp` 特别用 `InMemoryDexClassLoader`（ByteBuffer 加载）而不是 `DexClassLoader`，因为 `system_server` 受 SELinux 限制无法写优化 odex，DexClassLoader 会卡满看门狗窗口。

### 13.3 RootDeployer：把东西铺到设备上

**文件**：`service/Root/RootDeployer.kt`

| 常量 | 值 | 用途 |
| --- | --- | --- |
| `STAGING_DIR` | `/data/local/kail-lib` | 放注入器 + 应用内 hook so |
| `FAKELOC_DIR` | `/data/kail-loc` | 放 FakeLocation 全套 so + dex 载荷 |
| `NATIVE_HOOK_SO` | `libkail_native_hook.so` | 应用内传感器 hook |
| `INJECTOR_BIN` | `kail_inject` | ptrace 注入器 |

`ensureBaseline(context)`（每次服务启动都跑，幂等）：
```
prepareDirs()              # setenforce 0 + mkdir/chmod 777/chcon，预建 system_dex、oat
deployNativeHookLib()      # libkail_native_hook.so → /data/local/kail-lib
deployInjectorBin()        # kail_inject → /data/local/kail-lib（chmod 755）
deployFakelocLibs()        # 6 个 fakeloc so → /data/kail-loc（arm64 额外镜像出 *64.so）
deployDexPayload()         # 优先用 assets/inject.dex（精简，1~2MB），否则退回整个 APK
grantMockLocationAppOps()  # appops set <pkg> android:mock_location allow
bootstrapInjection()       # 尝试 kail_inject -P system_server -l libfakeloc_init.so
```

> **内容哈希校验（现场取哈希）**：文件名保持固定（`libfakeloc_init.so`、`kail_inject`、`libfakeloc.so` 等），不嵌入版本号或哈希。判断"是否最新"在 `isInjectionStaged()` 里现场取哈希：用 root `md5sum` 计算每个已部署文件的内容 md5，与当前 APK 内源的 md5 逐一对比，任一不一致就返回 false，`ensureBaseline` 随即 `resetDeployDirs()`（rm -rf 整个 `/data/kail-loc` 和 `/data/local/kail-lib`）后全量重部署。`injection_state.txt` / `zygote_state.txt` 记 `deploy_hash`（payload+注入器+init 加载器内容的组合哈希），so 内容一变哈希就变，强制重注入。若 `system_server` 已 mmap 某文件，重部署前先 rm 掉旧文件再写新文件（不原地截断，避免页故障崩溃）。

其它方法：`injectAppProcess(name)`（用 apphook 注入指定 App，例如把 `com.android.phone` 拉进来 hook 基站）、`isInjectionStaged()`（检查文件是否已就位）、`revokeMockLocationAppOps()`（退出 root 模式时撤权）。

### 13.4 InjectDex：注入成功后在目标进程里跑的 Java

**文件**：`inject/fakelocation/InjectDex.java`（被打进精简 dex）

三个反射入口对应三个加载器：

| 入口 | 由谁调 | 干什么 |
| --- | --- | --- |
| `init(context)` | `libfakeloc_init`（system_server） | 加载 `liblhooker`，注册 5 个 binder：`service_mock_location`(MockLocationManagerService)、`service_mock_wifi`、`service_mock_antidetection`、`service_hide_root`、`service_nativecatch` |
| `initZygote(startupParam)` | `libfakeloc_initzygote` | zygote 阶段加载 lhooker + `AppProcessHook.hook` |
| `hookApplication(context)` | `libfakeloc_apphook`（目标 App） | 普通 App → `AppProcessHook.applyHookToApp`；`com.android.phone` → `PhoneInterfaceManagerHook.hook`（基站） |

这些 binder 就是 `ServiceGoRoot` 里 `IMockLocationManager` / `IMockWifiManager` / `IHideRootManager` 等 AIDL 的服务端（AIDL 定义在 `inject/fakelocation/aidl/`，服务端实现在 `inject/fakelocation/service/`）。`ServiceGoRoot` 解析到这些 binder 后，就能调 `setMockCells` / `startStepSensorMock` / `setAllowMockPackages` 等。

### 13.5 LHooker：ART 方法 Hook

**文件**：`root/liblhooker.cpp`（C++）+ `lib/lhooker/LHooker`（Java 封装）

原理：改写目标 `ArtMethod` 的 `entry_point_from_quick_compiled_code` 字段，让方法调用跳到生成的 RWX 蹦床（trampoline），再跳到替换方法。`gEntryPointOffset` / `gArtMethodSize` 等偏移**按 Android API level（21~35）和 ABI 分别硬编码**（`LHooker_init` 里的大 switch）。这是版本最敏感的部分，换 ROM 要重新核对偏移。

### 13.6 应用内传感器 Hook（步频）

**文件**：`native_hook/hook.cpp` + `native_hook/sensor_simulator.{h,cpp}`，JNI 封装 `root/NativeSensorHook.kt`

- `NativeSensorHook.kt` 通过 `System.loadLibrary("kail_native_hook")` 加载，声明 10 个 external 方法：`nativeSetWriteOffset/ConvertOffset/RouteSimulation/GaitParams/ReloadConfig/SetMocking/SetAuthorized/SetStepSimEnabled/Reset/InitHook`。
- `hook.cpp` 用 **Dobby** 钩两个点：
  - `hooked_send_objects`：钩 `libsensor.so` 的批量事件发送，逐个改写 0x68 字节的 sensor event。
  - `hooked_convert_to_sensor_event`：钩 `libsensorservice.so`，按节奏注入"计步器(0x13)/步检测(0x12)"事件。
- 真正的步态波形由 `gait::SensorSimulator` 单例生成（傅里叶项 / 正弦+噪声两种方案，`GaitMode` 走/跑/快跑）。
- 偏移来自磁盘上 `libsensor.so`/`libsensorservice.so` 的符号探测，或读 `/data/local/tmp/kail_sensor_offsets.txt`。

> ⚠️ 关键限制（代码注释明确）：在**控制器 App 自己的进程**里安装这个 hook 是**空操作**——因为 `libsensor.so`/`libsensorservice.so` 映射在 `system_server` 和耗传感器的 App 里，不在 `com.kail.location` 里。要让 hook 真正生效，得靠 Xposed/Zygisk 把 so 加载进目标进程。所以 root 模式只 `ensureBaseline` 时步频会"优雅降级"。

### 13.7 反检测

**文件**：`root/libantidetect.cpp`（用 `elf_hooker.h`）

PLT/GOT 钩 libc 文件系统函数（`fopen`/`open`/`opendir` 等），在"mocking 开启"时把暴露框架自身的路径改写：含注册过的"反检测文件名"→ 乱码名；`.../maps`→`.../status`；`/su`→`/su_f`；`opendir(/sbin)`→`/etc`；`.../data/kail-loc`→`/data/_null`。JNI 面：`com.kail.location.inject.utils.LAntiDetect`。

---

## 14. Xposed 伴生模块

**工程**：`KailLocationXposed/`（独立 Gradle 工程，`com.kail.locationxposed`，minSdk 29）

### 14.1 模块入口

- `assets/xposed_init` 内容只有一行：`com.kail.locationxposed.xposed.core.FakeLocationXposed`
- **入口类**：`xposed/core/FakeLocationXposed.kt`，实现 `IXposedHookLoadPackage` + `IXposedHookZygoteInit`。
- Manifest 元数据：`xposedmodule=true`、`xposedminversion=93`、`xposedscope=@array/xposed_scope`。
- `de.robv.android.xposed:api:82` 是 `compileOnly`（运行时由 Xposed 框架提供）。

### 14.2 hook 哪些进程

`handleLoadPackage` 的 allow-list（同 `res/values/arrays.xml` 的 `xposed_scope`）：
`android`(system_server)、`com.android.phone`、`com.android.location.fused`、`com.google.android.gms`、`com.xiaomi.location.fused`、`com.oplus.location`、`com.vivo.location`、`com.qualcomm.location`、`com.tencent.android.location`。

进程内用 `System.getProperty("kail_location.injected_<pkg>")` 防重复注入；并解析 `ActivityThread` 的系统类加载器以便 hook 框架类。

按进程分发的 hook（节选）：
- `android`（system_server）：`LocationServiceHook`、`BasicLocationHook`、`LocationProviderManagerHook`、`LocationNMEAHook`、`LocationManagerHook`、`GnssHook`、Miui 系列、`AndroidFusedLocationProviderHook`、`WlanHook`、`TelephonyHook.hookSubOnTransact`、`SensorHookLite` 等。
- `com.android.phone`：`TelephonyHook`、`MiuiTelephonyManagerHook`。
- `com.android.location.fused` / 各 OEM fused：`AndroidFusedLocationProviderHook` / `ThirdPartyLocationHook(Lite)`。

### 14.3 主 App 与模块如何通信（重点）

**不是广播 / 文件 / socket，而是借道 `LocationManager.sendExtraCommand`。**

- 主 App `ServiceGoXposed`（`service/Xposed/ServiceGoXposed.kt`）：
  - `exchangeKey()`：`mLocManager.sendExtraCommand("kail", "exchange_key", extras)`，从返回 Bundle 取 `key`。
  - `sendXposedCommand(commandId, extras)`：把 `command_id` 放进 Bundle，再 `sendExtraCommand("kail", key, extras)`。
- 模块侧 `LocationServiceHook` hook 了 `sendExtraCommand`，当 `provider=="kail"` 转给 `xposed/core/KailCommandHandler.handle(...)`，用传入的 `out` Bundle 原地回传结果。

`KailCommandHandler` 支持的命令：`exchange_key`、`is_start`、`start`(altitude)、`stop`(复位所有开关)、`get_location`、`broadcast_location`、`set_speed`、`set_bearing`、`set_altitude`、`update_location`(lat/lon)、`load_library`、`set_step_enabled`/`set_step_cadence`/`set_step_sim_enabled`/`set_route_simulation`、`set_config`(批量开关)。

`startXposedMock()` 的调用序列：`start` → `update_location` → `set_config`(从 `PreferenceManager` 读各开关) → 步频系列命令。

### 14.4 状态与位置注入实现

- `xposed/core/FakeLocState.kt`：用 `AtomicReference` 存 enabled / location / speed / bearing / altitude / accuracy；`updateLocation(lat,lon)` 生成 `Location`。还负责从 `/data/local/kail-lib/libkail_native_hook.so` 加载 so（步频用）并应用 `/data/local/tmp/kail_sensor_offsets.txt` 偏移。
- `xposed/utils/FakeLoc.kt`：hook 读取的门面，holds 各 volatile 开关 + `jitterLocation()`（Ornstein-Uhlenbeck 自然抖动）/ `moveLocation()`。
- `xposed/base/BaseLocationHook.kt`：`injectLocation()` 用 FakeLoc 值造一个合成 `Location`，加抖动，按 `hideMock` 清除 mock 标记（`isMock=false`、反射清 `mMock`/`mIsFromMockProvider`），并塞伪卫星数等 extras。
- 各 provider hook（`LocationProviderManagerHook` 等）把系统上报的位置替换为合成位置。
- NMEA：`LocationNMEAHook` + `BaseLocationHook.injectNMEA`，改写 GGA/GNS/RMC 的经纬度。
- 基站：`TelephonyHook` 把网络降级为 CDMA，返回合成 `CellInfoCdma`，把基站经纬度编码为 `lat*14400 / lon*14400`。
- WiFi：`WlanHook` 钩 `WifiServiceImpl.getConnectionInfo/getScanResults`，返回假 MAC `02:00:00:00:00:00` / 空扫描列表。

---

## 15. 数据存储与网络

### 15.1 本地存储

| 类型 | 位置 | 用途 |
| --- | --- | --- |
| SharedPreferences（默认） | `PreferenceManager.getDefaultSharedPreferences` | 运行模式、各种开关、参数、保存的路线(JSON)、独立模拟白名单 |
| SQLite（原生） | `repositories/DataBaseHistoryLocation.kt`、`DataBaseHistorySearch.kt` | 历史位置 / 搜索记录 |
| Room | `data/local/`（KSP 生成） | 结构化历史栈 |
| 文件 | `getExternalFilesDir("Logs")` | 崩溃日志；`/storage/.../Documents/KailLocation/logs/` 运行日志 |

历史记录仓库：`repositories/HistoryRepository.kt`、`RootMockRepository.kt`。数据模型在 `models/`（`HistoryRecord`、`RouteModels`、`CellInfo`、`WifiInfo`、`UpdateInfo`）。

### 15.2 网络（OkHttp）

`network/` 下三个客户端：
- `RuoYiClient.kt`：后端鉴权 / 用量接口（`checkSimulation` / `useSimulation`）。后端地址在 `app/build.gradle.kts` 的 `buildConfigField`：`ADMIN_API_URL` / `APP_API_URL`（`https://adminkaillocation.kaillocation.xyz/...`）。
- `OpenCellIdClient.kt`：基站数据库查询（基站模拟用）。
- `WigleClient.kt`：WiFi 数据库查询（WiFi 模拟用）。

更新检查：`utils/UpdateChecker.kt`，更新弹窗 `views/common`，下载在各 ViewModel（如 `RouteSimulationViewModel.startUpdateDownload`），用 `FileProvider` 触发安装。

---

## 16. 坐标系与地理计算

项目内部统一用 **WGS84**，UI/百度地图用 **BD09**，部分数据是 **GCJ02**。转换工具在 `utils/MapUtils.kt`：

- `bd2wgs(lng,lat)` / `wgs2bd(lng,lat)`：BD09 ↔ WGS84
- `gcj02towgs84(lng,lat)`：GCJ02 → WGS84

服务在 `onStartCommand` 里根据 `EXTRA_COORD_TYPE` 把输入坐标统一转成 WGS84 再用。

地理数学在 `geo/`：
- `GeoMath.kt`：`bearingDegrees`（两点航向）、`metersPerDegLat/Lng`（每度多少米）。
- `GeoPredict.kt`：`nextByDisplacementKm`（按位移推算下一个坐标，摇杆用）、`randomInRangeWithMean`（速度波动）。

NMEA 报文解析在 `nmea/NMEA.kt` + `NmeaValue.kt`（GGA/GNS/RMC/GSA/GSV/VTG/DTM）。

---

## 17. 登录鉴权与用量限制

**文件**：`auth/AuthManager.kt`（登录态/token/订阅）、`auth/UsageManager.kt`（用量）。

**每次开始模拟前都会过用量闸门**（在各 ViewModel 的 start 逻辑里）：

```
UsageManager.canStartSimulation(context):   # 只检查，不扣次数
  未登录            → Toast"请先登录" → false
  已订阅            → true
  否则 RuoYiClient.checkSimulation(token):
      剩余次数 <= 0 → Toast"今日免费次数用完" → false
      否则          → true

UsageManager.consumeSimulation(context):    # 真正开始时扣一次
  已订阅 → true（不扣）
  否则 RuoYiClient.useSimulation(token)
```

所以你会在 `LocationSimulationViewModel.toggleSimulation`、`RouteSimulationViewModel.startSimulation`、`IndependentSimulationViewModel.setEnabled` 等处看到先 `canStartSimulation` 再 `consumeSimulation` 的固定套路。未登录或次数耗尽会直接 `return`，服务不会启动。

---

## 18. 新人上手路线图

按这个顺序读代码，能最快建立全局认知：

1. **跑起来**：`./gradlew installDebug`（若同步失败先处理 `NewBlackbox` 子模块缺失，见 §2/§4）。
2. **入口**：`utils/GoApplication.kt` → `views/welcome/WelcomeActivity.kt` → `views/locationsimulation/LocationSimulationActivity.kt`。搞清启动 3 步。
3. **一条完整链路**：从 `LocationSimulationScreen` 的"开始"按钮，追到 `LocationSimulationViewModel.toggleSimulation`，再到 `ServiceGoDeveloper.onStartCommand` → `MockLocationProvider.setLocation`（§9）。这是理解全项目的钥匙。
4. **共享设施**：`utils/service/ServiceConstants.kt`、`RouteEngine.kt`、`ServiceNotificationHelper.kt`、`views/joystick/JoystickWindowManager.kt`（§12）。
5. **模式差异**：对比 `service/Developer` 与 `service/Sandbox`（最简单的两种），再看 `service/Root`（最复杂）、`service/Xposed`。
6. **想碰 Native 再看**：`cpp/CMakeLists.txt` → `service/Root/RootDeployer.kt` → `inject/fakelocation/InjectDex.java` → `cpp/root/*.cpp`（§13）。Native 改动风险高，先读注释里的"为什么"。
7. **Xposed**：`KailLocationXposed/` 工程，从 `FakeLocationXposed.kt` 和 `KailCommandHandler.kt` 入手（§14）。

**改某个功能时的定位技巧**：
- 改界面/交互 → `views/<feature>/`
- 改业务逻辑/状态 → `viewmodels/<Feature>ViewModel.kt`
- 改"模拟怎么下发" → `service/<Mode>/`
- 改 Intent 常量/默认坐标 → `utils/service/ServiceConstants.kt`
- 改注入/底层 hook → `cpp/` + `inject/`

---

## 19. 术语表

| 术语 | 含义 |
| --- | --- |
| 运行模式 / runMode | `root`/`developer`/`sandbox`/`xposed`，存于 `setting_run_mode` |
| 前台服务 | 4 个 `ServiceGoXxx`，真正执行模拟的常驻组件 |
| 测试 Provider | `LocationManager.addTestProvider`，Android 官方模拟位置接口（developer/root 用） |
| BlackBox / 沙盒 | NewBlackbox 提供的应用虚拟化环境（sandbox 模式用） |
| ptrace 注入 | `kail_inject` 用 ptrace 把 .so 加载进目标进程（root 模式用） |
| InjectDex | 注入成功后在目标进程里运行的 Java 钩子框架入口 |
| LHooker | 自研 ART 方法 Hook 引擎（改 ArtMethod entry point） |
| Dobby / ShadowHook | inline hook 框架（应用内传感器 hook 用 Dobby） |
| FakeLocState / FakeLoc | Xposed 模块里保存模拟状态 / 开关的对象 |
| sendExtraCommand("kail") | 主 App ↔ Xposed 模块的通信通道 |
| 精简 dex / inject.dex | 构建期生成、只含注入相关类的小 dex，注入 system_server 用 |
| WGS84 / GCJ02 / BD09 | 三种坐标系，内部用 WGS84，百度用 BD09 |

---

> 文档基于当前代码静态梳理而成；Native 注入链与 ART/传感器偏移高度依赖具体 ROM 与 Android 版本，实操前请以代码注释中的告警为准，并在目标设备上验证。

---

## 20. 近期代码更新（基于最近 git 提交）

> 本节汇总文档初版之后、最近若干次提交（`git log`，提交信息均为 "update"）引入的重要变化。已合并进上文相关章节的概念，这里集中说明"新增/改动了什么、在哪、为什么"，方便老读者增量跟进。

### 20.1 LHooker 自适应 ArtMethod 布局探测（重要）

**新增文件**：`lib/lhooker/ArtMethodProbe.java`
**改动文件**：`cpp/root/liblhooker.cpp`、`lib/lhooker/LHooker.java`

之前（§13.5）LHooker 依赖一张"按 SDK 21~35 硬编码"的 `ArtMethod` 偏移表，遇到未登记的新 Android 版本会用错偏移、把 `system_server` 写崩。现在改为**运行时自测量**：

- `ArtMethodProbe` 是一个只含 `m0()`~`m11()` 共 12 个空静态方法的探针类。ART 里同一个类声明的方法在内存中是连续排布的 `LengthPrefixedArray<ArtMethod>`，`jmethodID` 就是 `ArtMethod*`。
- Native 侧 `probeArtMethodLayout()`（`liblhooker.cpp`）解析这些方法的指针，取**两两之间最小正间距**即为当前设备真实的 `sizeof(ArtMethod)`；quick entry point 是最后一个指针大小字段，偏移 = `size - sizeof(void*)`。
- `init()` 里**优先用探测值覆盖静态表**，于是引擎对任意 ROM（含未来版本）都能自配置，不再因偏移过时而损坏 `system_server`。
- 探针类必须保持空方法体、不加构造器/字段、不被裁剪——精简 dex 用 D8 且不做 shrink，正好原样保留。

**配套诊断**：新增 native 方法 `LHooker.nativeGetInitLog()`，把"布局探测结果 + 最终偏移 + 是否失败"汇总成文本；`LHooker.loadHookLibrary` 在 `init()` 后调用 `persistInitLog()` 通过 `InjectLog` 落盘，方便在拿不到设备时离线排查。

### 20.2 注入态日志体系（InjectLog + kail_log.h）

**新增文件**：`inject/utils/InjectLog.java`、`cpp/kail_log.h`

注入到目标进程后运行的 Hook 代码读不到本 App 的 SharedPreferences，原来的 `log(...)` 桩方法是空的、几百处埋点全失效。现在：

- **Java/注入侧 `InjectLog`**：统一实现，带"文件:行号#方法"定位；开关用**公共目录标记文件**控制（无需 IPC / 重打包，对所有目标进程统一生效）：
  - `/sdcard/Documents/KailLocation/logs/.kail_debug` → 开启日志
  - `/sdcard/Documents/KailLocation/logs/.kail_log_file` → 额外落盘
  - `/sdcard/Documents/KailLocation/logs/.kail_verbose` → 开启高频(V)日志
  - 标记文件带 TTL 缓存（默认 5s）；W/E 始终输出，高频 V 日志限流并标注 `(+N suppressed)`；不使用 `SimpleDateFormat` 以免在 native/系统线程触发 ICU 崩溃。
- **Native 侧 `kail_log.h`**：统一日志宏 `KLOGV/KLOGD/KLOGI/KLOGW/KLOGE`，自动附加 `[file:line#func]`；高频 `KLOGV` 受 `kail::setVerboseLogging(bool)` 运行时开关控制，默认关闭避免热点路径刷屏。各 native 源（hook.cpp / elf_hooker.h / inject*.cpp / libStepSensor.cpp / liblhooker.cpp 等）已接入。

### 20.3 Root 步频改走 NativeStepHook（替代易崩的 libStepSensor）

**新增文件**：`inject/utils/NativeStepHook.java`
**配套**：`native_hook/hook.cpp`、`sensor_simulator.{h,cpp}` 增强

新增 `NativeStepHook`，作为**注入态**到 `libkail_native_hook.so` 的桥（与 §13.6 的应用内 `root/NativeSensorHook.kt` 是不同包、面向不同进程的两座桥）：

- 由 `InjectDex` 在 `system_server` 内 `System.load("/data/kail-loc/libkail_native_hook.so")` 后使用——`system_server` 才真正映射了 `libsensorservice.so`，所以合成的计步/步检测事件能进入全局传感器流。
- 用 **Dobby inline hook**（`convertToSensorEvent` / `send_objects`）而非原 `libStepSensor` 的 ELF PLT hook（后者会让 SensorService 崩溃）。
- JNI 方法：`nativeSetWriteOffset/ConvertOffset`、`nativeSetGaitParams`、`nativeSetRouteSimulation`、`nativeSetMocking`、`nativeSetStepSimEnabled`、`nativeReset`、`nativeInitHook`（符号导出在 `hook.cpp` 的 `Java_com_kail_location_inject_utils_NativeStepHook_*`）。
- 典型用法：`ensureLoaded()` → `install(writeOffset, convertOffset, spm)`（装 hook）→ `start(spm, mode, scheme)` / `stop()`。
- `sensor_simulator` 侧新增 `RebaseStepSegment`（按节奏分段重定基，步频切换更平滑）与 `seg_spm_` 等字段，步态波形更真实。

> 现状理解：root 模式步频有两条路径——应用内 `NativeSensorHook`（多为占位/降级，因为本进程没映射 libsensorservice）与注入态 `NativeStepHook`（注入 `system_server` 后才真正生效）。后者是这次更新的主路径。

### 20.4 数据导出/导入（备份恢复）

**新增文件**：`utils/DataTransferManager.kt`，配套 `views/settings/SettingsScreen.kt` 入口

新增按"运行模式分组 + 菜单类别"组织的备份/恢复能力：

- 产物是 JSON 文本（后缀 `.bak`），头部 `format=kail_location_backup`、`version=1`、`exportedAt`、`appVersion`，`categories` 下按类别存。
- **类别分组**（`ModeGroup`）：
  - GENERAL（所有模式共享）：位置模拟（**SQLite**：`HistoryLocation.db`）、设置（`setting_*` 等）、路线模拟（`route_sim_*` / `saved_routes`）、NFC（独立 prefs `nfc_history`）。
  - ROOT：独立模拟（`independent_*`）、WiFi（`wifi_sim_*`）、基站（`cell_sim_*`）。
  - SANDBOX：沙盒引擎（BlackBox 的 `AppSharedPreferenceDelegate` prefs，显式 key：`mHideRoot`/`mDaemonEnable`/`mUseVpnNetwork`/`mDisableFlagSecure`）。
  - XPOSED：Xposed 设置页专属一组 `setting_*` 显式 key。
- 每个值保留原类型（`s/b/i/f/l/ss`）。SQLite 类别按行序列化，导入时不保留自增 ID、以 WGS84 经纬度为去重键。
- 主要 API：`availableEntryCounts()`（提示哪些菜单有数据）、`export(uri, ids)`、`parseBackup(uri)`、`import(backup, ids)`。新增菜单数据时只需在 `categories` 列表登记一条 `Category`。

### 20.5 Root / 应用隐藏功能落地

**改动文件**：`viewmodels/RootAppHideViewModel.kt`（+119 行实现）、`views/roothide/RootAppHideScreen.kt`（新 226 行 UI）、`views/roothide/RootAppHideActivity.kt`

§11.4 提到的"Root/应用隐藏"从占位变成了真实现：通过 `ServiceGoRoot` 的 `EXTRA_HIDE_*` / `CONTROL_SET_HIDE` / `CONTROL_STOP_HIDE` 把隐藏白名单与开关下发到注入层 `service_hide_root`（`IHideRootManager`），对目标 App 隐藏 Root / 已安装列表 / 注入痕迹。`ServiceGoRoot` 本轮也大量扩充（WiFi/基站/独立模拟/隐藏的控制分支，见 §8.2 与 §13.4）。

### 20.6 其它

- **多语言**：新增 `values-en` / `values-ja` / `values-zh-rHK` / `values-zh-rTW` 的 `strings.xml`，并扩充默认 `strings.xml`（数据导出分组等文案）。
- **网络客户端**：`OpenCellIdClient` / `RuoYiClient` / `WigleClient` 有改动（接口/错误处理调整）。
- **沙盒**：`SandboxManager` / `SandboxSettingsManager` / `SandboxStepConfig` / `SandboxLocationHook` 小幅增强。
- **Xposed 模块**：`xposed/utils/KailLog.kt` 大幅增强（与上面的统一日志风格对齐），`SensorHookLite`、Miui 系列 hook 有微调。
- **构建**：仓库新增了 `app/release/` 下的 `baselineProfiles` 与 `output-metadata.json`（release 产物/基线配置）；`app/build.gradle.kts` 微调。

> 如需逐行核对，可用 `git -C <repo> show <commit>:<path>` 或 `git diff <oldCommit> <newCommit> -- <path>`。最近这串提交里，`00d35bc`（接入统一日志 + 注入态步频 + 大量 inject 类生效）和 `e890d9d`（步态模拟增强 + 多处 ViewModel/UI 调整）改动面最大。
