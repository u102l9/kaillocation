# 路线模拟：速度 / 步频 随机区间（改动说明）

## 一、改了什么

路线模拟时，目标 App 读到的**配速**和**步频**不再是一条直线，而是在你设定的区间内周期性随机取值。

### 问题的根源

原来的实现里，`speedFluctuation` 开关只随机了「推进路线用的位移速度」：

```kotlin
// 旧代码 ServiceGoRoot.kt
val speedForStep = if (speedFluctuation) {
    GeoPredict.randomInRangeWithMean(mSpeed * 0.5, mSpeed * 1.5, mSpeed)
} else { mSpeed }
mRouteEngine.advance(speedForStep * (elapsedMs / 1000.0))
```

但**上报给目标 App 的 `speed` 字段**（写进控制文件 / ashmem 的那个）始终是基准值 `mSpeed`，一步都没随机过；步频 `stepCadence` 更是全程固定，只在启动和手动改动时才写一次。

所以你看到的现象是：位移有抖动，但**配速读数恒定、步频恒定**——很假。

### 现在的做法

每 `变化间隔` 秒从区间内重新抽一次值，抽到的值**同时**用于：

1. 推进路线（`mRouteEngine.advance`）
2. 上报字段（控制文件的 `speed=` / ashmem 的 speed / 步频 `step_spm`）

## 二、怎么用

路线模拟 → 右上角设置弹窗：

| 控件 | 说明 |
| --- | --- |
| 速度浮动（开关） | 打开后出现区间设置 |
| 速度随机区间 | 最小值 / 最大值两个滑杆，单位 km/h，范围 0–200 |
| 步频模拟（开关） | root / xposed / sandbox 模式才可用 |
| 步频随机（开关） | 打开后出现区间设置 |
| 步频随机区间 | 最小 / 最大步频，单位 步/分钟，范围 60–240 |
| 变化间隔 | 每隔多少秒重抽一次，1–120 秒 |

**跑步场景推荐**：步频 `150 ~ 175`、速度 `8 ~ 11 km/h`、变化间隔 `10 ~ 20 秒`。

变化间隔别设太小（< 5 秒）：C++ 侧步频平滑用的是 τ=1.2s 的一阶滤波，变得太快会一直在追，反而显得抖。10 秒以上最自然。

## 三、改动的文件

| 文件 | 改动 |
| --- | --- |
| `models/RouteModels.kt` | `SimulationSettings` 新增 6 个字段：`speedMin/speedMax`、`stepCadenceFluctuation`、`stepCadenceMinSpm/stepCadenceMaxSpm`、`randomIntervalSec` |
| `service/Root/ServiceGoRoot.kt` | 核心逻辑：新增 `readRandomRangeExtras` / `normalizeRandomRange` / `maybeRandomize` / `applyRandomizedValues` / `pushStepConfigToProvider`；位置循环接入；上报的 speed 字段改用随机值 |
| `viewmodels/RouteSimulationViewModel.kt` | 读取 / 持久化新设置；启动与运行时（`CONTROL_SET_RANDOM_RANGE`）下发区间给服务 |
| `views/routesimulation/RouteSimulationScreen.kt` | 设置弹窗新增区间滑杆，新增 `RangeRow` 双滑杆组件 |
| `res/values/strings.xml`、`values-en/strings.xml` | 新增 8 条字符串（其他语种自动回退到默认中文） |

## 四、几个实现细节

**1. 步频两条通道都要更新**

步频配置有两条下发路径：
- 控制文件（`step_spm=`，system_server 轮询消费）
- Provider（`LocationShmProvider.setConfig(KEY_STEP_CONFIG)`，system_server 每 1 秒 call 一次）

原来只有走控制文件路径时才会更新 Provider。而走 **ashmem 快速路径**时 `pushLocationToInjection()` 会提前 return，根本不碰控制文件——**步频永远不更新**。所以新增的 `pushStepConfigToProvider()` 直接写 Provider，两条路径都覆盖到了。

**2. 步频随机不会丢步数**

C++ 侧 `StepsAt()` 是分段积分（`base + 经过秒数 × spm/60`），改步频时会先 `RebaseStepSegment()` 把已累计的步数存进 `step_base_` 再重开计时段。所以中途改步频不会丢步、不会重复计数，计数始终单调递增。

**3. 未开启随机时行为完全不变**

`currentSpeedMps` 在不开启随机时始终等于 `mSpeed`；摇杆模式下也会跟着 `mSpeed` 同步。不开启任何随机开关 = 和原版一致。

**4. 只在 root 模式实现**

`pushRandomRangeToService()` 里限定了 `_runMode.value != "root"` 直接返回。xposed / sandbox 模式的服务（`ServiceGoXposed`、`ServiceGoSandbox`）没改，它们的步频仍是固定值。

**5. 变化在 1 秒内生效**

system_server 的 `KailProviderConfigPoller` 活跃时 1000ms 轮询一次 Provider；控制文件则是每个上报周期（默认 `setting_report_interval`）写一次。所以改完设置最多 1~2 秒就能看到数据变化。

## 五、已知限制

- **沙盒 / Xposed 模式未实现**：只有 root 模式的 `ServiceGoRoot` 接了随机逻辑。
- **正弦方案（scheme=1）的频率仍受 `effective = 180 - spm` 影响**：这是原有的另一个独立问题（`sensor_simulator.cpp:264`）。本次没动它。如果你用正弦且想让波形频率也跟着区间走，需要另外把那三处改成 `effective_spm = current_spm_`。
- **未做真机验证**：沙盒无 Android SDK，改动只经过静态检查（括号平衡、语法审阅），没有编译与实机测试。
